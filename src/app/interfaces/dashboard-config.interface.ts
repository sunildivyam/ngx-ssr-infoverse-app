import { MetaInfo } from "@annuadvent/ngx-common-ui/meta";

export interface DashboardConfig {
    dashboardPage?: MetaInfo;
    storiesHomePage?: MetaInfo;
    storyPage?: MetaInfo;
    categoriesPage?: MetaInfo;
    categoryPage?: MetaInfo;
    sitemapPage?: MetaInfo;
    openaiPage?: MetaInfo;
};
